spring:
  application:
    name: SecurientCRMapp
  security:
    enabled: false
    user:
      name: admin
      password: 123
  datasource:
    url: jdbc:h2:mem:securientcrm
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create

#server:
#  servlet:
#    context-path: /api/v1
